<Window x:Class="OverWatchELD.Views.EditDutyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Edit Duty Segment (DOT Annotation Required)"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#07111F">

    <Window.Resources>

        <SolidColorBrush x:Key="EldBackground" Color="#07111F"/>
        <SolidColorBrush x:Key="EldPanel" Color="#0D1A2B"/>
        <SolidColorBrush x:Key="EldInput" Color="#132238"/>
        <SolidColorBrush x:Key="EldBorder" Color="#385476"/>
        <SolidColorBrush x:Key="EldText" Color="#EAF2FF"/>
        <SolidColorBrush x:Key="EldMuted" Color="#9FB3CC"/>
        <SolidColorBrush x:Key="EldBlue" Color="#163B65"/>
        <SolidColorBrush x:Key="EldBlueBorder" Color="#4A91D0"/>

        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource EldText}"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Foreground" Value="{StaticResource EldText}"/>
            <Setter Property="Background" Value="{StaticResource EldInput}"/>
            <Setter Property="BorderBrush" Value="{StaticResource EldBorder}"/>
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Padding" Value="8,5"/>
        </Style>

        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource EldText}"/>
            <Setter Property="Background" Value="{StaticResource EldInput}"/>
            <Setter Property="Padding" Value="8,5"/>

            <Style.Triggers>

                <Trigger Property="IsHighlighted" Value="True">
                    <Setter Property="Background" Value="#245B8F"/>
                </Trigger>

                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#1F7A4D"/>
                </Trigger>

            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">

            <Setter Property="Height" Value="32"/>
            <Setter Property="Foreground" Value="#EAF2FF"/>
            <Setter Property="Background" Value="#132238"/>
            <Setter Property="BorderBrush" Value="#385476"/>
            <Setter Property="Padding" Value="8,3"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>

                            <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4">

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="34"/>
                                    </Grid.ColumnDefinitions>

                                    <ContentPresenter Margin="10,0,0,0"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Left"
                                              Content="{TemplateBinding SelectionBoxItem}"
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"/>

                                    <Border Grid.Column="1"
                                    Background="#163B65"
                                    BorderBrush="#4A91D0"
                                    BorderThickness="1,0,0,0">
                                        <TextBlock Text="▼"
                                           Foreground="White"
                                           FontSize="10"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"/>
                                    </Border>

                                    <ToggleButton Grid.ColumnSpan="2"
                                          Background="Transparent"
                                          BorderThickness="0"
                                          Focusable="False"
                                          ClickMode="Press"
                                          IsChecked="{Binding IsDropDownOpen,
                                                              RelativeSource={RelativeSource TemplatedParent},
                                                              Mode=TwoWay}"/>
                                </Grid>
                            </Border>

                            <Popup x:Name="PART_Popup"
                           Placement="Bottom"
                           IsOpen="{TemplateBinding IsDropDownOpen}"
                           AllowsTransparency="True"
                           Focusable="False"
                           PopupAnimation="Slide">

                                <Border Background="#132238"
                                BorderBrush="#385476"
                                BorderThickness="1"
                                MinWidth="{TemplateBinding ActualWidth}">
                                    <ScrollViewer MaxHeight="220">
                                        <ItemsPresenter/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>

                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>

        </Style>

        <Style TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource EldBlue}"/>
            <Setter Property="BorderBrush" Value="{StaticResource EldBlueBorder}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Padding" Value="12,4"/>
        </Style>

    </Window.Resources>

    <Border Background="#0D1A2B"
            BorderBrush="#263E5C"
            BorderThickness="1"
            CornerRadius="10"
            Padding="16"
            Margin="12"
            Width="520">

        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       FontSize="16"
                       FontWeight="SemiBold"
                       Foreground="#EAF2FF"
                       Text="{Binding HeaderText}"
                       Margin="0,0,0,12"/>

            <Grid Grid.Row="1" Margin="0,0,0,10">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Grid.Column="0"
                           Text="New Duty Status:"
                           Foreground="#9FB3CC"
                           VerticalAlignment="Center"/>

                <ComboBox Grid.Row="0"
                          Grid.Column="1"
                          ItemsSource="{Binding AllowedStatuses}"
                          SelectedItem="{Binding SelectedStatus}"
                          Margin="0,2,0,8"/>

                <TextBlock Grid.Row="1"
                           Grid.Column="0"
                           Text="Start Time (local):"
                           Foreground="#9FB3CC"
                           VerticalAlignment="Center"/>

                <TextBox Grid.Row="1"
                         Grid.Column="1"
                         Text="{Binding StartLocalText, Mode=OneWay}"
                         IsReadOnly="True"
                         Margin="0,2,0,8"/>

                <TextBlock Grid.Row="2"
                           Grid.Column="0"
                           Text="End Time (local):"
                           Foreground="#9FB3CC"
                           VerticalAlignment="Center"/>

                <TextBox Grid.Row="2"
                         Grid.Column="1"
                         Text="{Binding EndLocalText, Mode=OneWay}"
                         IsReadOnly="True"
                         Margin="0,2,0,0"/>

            </Grid>

            <StackPanel Grid.Row="2" Margin="0,0,0,12">

                <TextBlock Text="DOT Annotation / Reason (required):"
                           Foreground="#9FB3CC"
                           Margin="0,0,0,6"/>

                <TextBox Text="{Binding NoteText}"
                         AcceptsReturn="True"
                         Height="90"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"/>

                <TextBlock Text="{Binding ErrorText}"
                           Foreground="Tomato"
                           Margin="0,6,0,0"/>

            </StackPanel>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">

                <Button Content="Cancel"
                        Width="92"
                        Margin="0,0,10,0"
                        Background="#2A3444"
                        BorderBrush="#4E6077"
                        Command="{Binding CancelCommand}"/>

                <Button Content="Save"
                        Width="92"
                        Command="{Binding SaveCommand}"/>

            </StackPanel>

        </Grid>

    </Border>

</Window>