<Window x:Class="OverWatchELD.Views.UpdateDownloadWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OverWatch ELD Update"
        Height="190"
        Width="470"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Background="#0B1220"
        Foreground="#E7EEF8"
        ShowInTaskbar="False">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="14"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Downloading Update"
                   FontSize="22"
                   FontWeight="Bold"/>

        <TextBlock x:Name="StatusText"
                   Grid.Row="2"
                   Text="Preparing download..."
                   FontSize="13"
                   Foreground="#AFC0D7"
                   TextWrapping="Wrap"/>

        <ProgressBar x:Name="Progress"
                     Grid.Row="4"
                     Height="22"
                     Minimum="0"
                     Maximum="100"
                     Value="0"/>
    </Grid>
</Window>