<UserControl x:Class="OverWatchELD.Views.FleetManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#111111"
             Foreground="White">
    <Grid Margin="12" Background="#111111">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Fleet Management"
                       FontSize="20"
                       FontWeight="SemiBold"/>
            <TextBlock x:Name="StatusText"
                       Text="Manage VTC fleet trucks and link them to Discord users."
                       Opacity="0.75"
                       Margin="0,3,0,0"/>
        </StackPanel>

        <!-- Search / quick actions -->
        <Border Grid.Row="1"
                Background="#161616"
                CornerRadius="10"
                Padding="12"
                Margin="0,0,0,10"
                BorderBrush="#2A2A2A"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.2*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical" Margin="0,0,12,0">
                    <TextBlock Text="Search Fleet"
                               Foreground="#CFCFCF"
                               Margin="0,0,0,4"/>
                    <TextBox x:Name="SearchTextBox"
                             Height="34"
                             VerticalContentAlignment="Center"
                             Background="#202020"
                             Foreground="White"
                             BorderBrush="#333333"
                             TextChanged="SearchTextBox_TextChanged"/>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            VerticalAlignment="Bottom">
                    <Button x:Name="RefreshFleetButton"
                            Content="Refresh"
                            Margin="0,0,8,0"
                            Padding="14,8"
                            Click="RefreshFleetButton_Click"/>
                    <Button x:Name="SetActiveButton"
                            Content="Set Selected Active"
                            Padding="14,8"
                            Click="SetActiveButton_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Entry row -->
        <Border Grid.Row="2"
                Background="#1A1A1A"
                CornerRadius="10"
                Padding="12"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="TruckNameTextBox"
                         Grid.Column="0"
                         Margin="0,0,8,0"
                         Height="34"
                         VerticalContentAlignment="Center"
                         Background="#202020"
                         Foreground="White"
                         BorderBrush="#333333"/>

                <TextBox x:Name="ModelTextBox"
                         Grid.Column="1"
                         Margin="0,0,8,0"
                         Height="34"
                         VerticalContentAlignment="Center"
                         Background="#202020"
                         Foreground="White"
                         BorderBrush="#333333"/>

                <TextBox x:Name="PlateTextBox"
                         Grid.Column="2"
                         Margin="0,0,8,0"
                         Height="34"
                         VerticalContentAlignment="Center"
                         Background="#202020"
                         Foreground="White"
                         BorderBrush="#333333"/>

                <TextBox x:Name="DriverTextBox"
                         Grid.Column="3"
                         Margin="0,0,8,0"
                         Height="34"
                         VerticalContentAlignment="Center"
                         IsReadOnly="True"
                         Background="#181818"
                         Foreground="White"
                         BorderBrush="#333333"/>

                <Button Grid.Column="4"
                        x:Name="AddTelemetryButton"
                        Content="Add Current Truck"
                        Margin="0,0,8,0"
                        Padding="14,8"
                        Click="AddTelemetryButton_Click"/>

                <Button Grid.Column="5"
                        x:Name="AddManualButton"
                        Content="Add Manual"
                        Margin="0,0,8,0"
                        Padding="14,8"
                        Click="AddManualButton_Click"/>

                <Button Grid.Column="6"
                        x:Name="DeleteButton"
                        Content="Delete Selected"
                        Padding="14,8"
                        Click="DeleteButton_Click"/>
            </Grid>
        </Border>

        <!-- Fleet grid -->
        <Border Grid.Row="3"
                Background="#111111"
                BorderBrush="#2A2A2A"
                BorderThickness="1"
                CornerRadius="8">
            <DataGrid x:Name="FleetGrid"
                      AutoGenerateColumns="False"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      Background="#111111"
                      Foreground="White"
                      BorderThickness="0"
                      RowBackground="#111111"
                      AlternatingRowBackground="#151515"
                      HorizontalGridLinesBrush="#222222"
                      VerticalGridLinesBrush="#222222"
                      GridLinesVisibility="Horizontal"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      SelectionChanged="FleetGrid_SelectionChanged"
                      MouseDoubleClick="FleetGrid_MouseDoubleClick">
                <DataGrid.Resources>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="#1A1A1A"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderBrush" Value="#2A2A2A"/>
                        <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="FontWeight" Value="SemiBold"/>
                    </Style>

                    <Style TargetType="DataGridCell">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="8,6"/>
                    </Style>

                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="#111111"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="SnapsToDevicePixels" Value="True"/>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#1E2A38"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                <Setter Property="Background" Value="#16261A"/>
                            </DataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsActive}" Value="True"/>
                                    <Condition Binding="{Binding RelativeSource={RelativeSource Self}, Path=IsSelected}" Value="True"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" Value="#24412A"/>
                                <Setter Property="Foreground" Value="White"/>
                            </MultiDataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Truck Name" Binding="{Binding TruckName}" Width="1.6*"/>
                    <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="1.5*"/>
                    <DataGridTextColumn Header="Plate" Binding="{Binding PlateNumber}" Width="1.0*"/>
                    <DataGridTextColumn Header="Linked Driver" Binding="{Binding DiscordUsername}" Width="1.4*"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding FleetStatus}" Width="1.0*"/>
                    <DataGridTextColumn Header="Odometer" Binding="{Binding OdometerMiles}" Width="0.9*"/>
                    <DataGridTextColumn Header="Last Seen UTC" Binding="{Binding LastSeenUtc}" Width="1.4*"/>
                    <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="0.7*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>