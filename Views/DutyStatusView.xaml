<UserControl x:Class="OverWatchELD.Views.DutyStatusView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             Background="Transparent">

    <UserControl.Resources>

        <SolidColorBrush x:Key="DutyTileBg" Color="#1A2433"/>
        <SolidColorBrush x:Key="DutyTileBgChecked" Color="#1A2433"/>
        <SolidColorBrush x:Key="DutyTileBorder" Color="#2A3B55"/>
        <SolidColorBrush x:Key="DutyTileBorderChecked" Color="#3B82F6"/>
        <SolidColorBrush x:Key="DutyTileText" Color="#E7EEF8"/>
        <SolidColorBrush x:Key="DutyTileTextChecked" Color="#3B82F6"/>
        <SolidColorBrush x:Key="DutyTileLabel" Color="#AFC0D7"/>

        <Style x:Key="DutyTileStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{StaticResource DutyTileBg}" />
            <Setter Property="BorderBrush" Value="{StaticResource DutyTileBorder}" />
            <Setter Property="Foreground" Value="{StaticResource DutyTileText}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Padding" Value="14,10" />
            <Setter Property="HorizontalContentAlignment" Value="Center" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter Margin="{TemplateBinding Padding}"
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="{StaticResource DutyTileBorderChecked}" />
                                <Setter Property="Foreground" Value="{StaticResource DutyTileTextChecked}" />
                            </Trigger>

                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="BorderBrush" Value="#60A5FA"/>
                            </Trigger>

                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.55"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DutyTileWideStyle"
               TargetType="ToggleButton"
               BasedOn="{StaticResource DutyTileStyle}">
            <Setter Property="Padding" Value="14,12"/>
        </Style>

    </UserControl.Resources>

    <StackPanel>

        <TextBlock Text="Duty Status *"
                   FontSize="12"
                   FontWeight="SemiBold"
                   Foreground="{StaticResource DutyTileLabel}"
                   Margin="0,0,0,8"/>

        <UniformGrid Columns="4" Margin="0,0,0,10">
            <ToggleButton Style="{StaticResource DutyTileStyle}"
                          Content="OFF"
                          IsChecked="{Binding IsOffDuty, Mode=TwoWay}"
                          Margin="0,0,10,0"/>

            <ToggleButton Style="{StaticResource DutyTileStyle}"
                          Content="SB"
                          IsChecked="{Binding IsSleeperBerth, Mode=TwoWay}"
                          Margin="0,0,10,0"/>

            <ToggleButton Style="{StaticResource DutyTileStyle}"
                          Content="D"
                          IsChecked="{Binding IsDriving, Mode=TwoWay}"
                          Margin="0,0,10,0"/>

            <ToggleButton Style="{StaticResource DutyTileStyle}"
                          Content="ON"
                          IsChecked="{Binding IsOnDuty, Mode=TwoWay}"/>
        </UniformGrid>

        <UniformGrid Columns="2">
            <ToggleButton Style="{StaticResource DutyTileWideStyle}"
                          Content="Personal&#x0a;Conveyance"
                          IsChecked="{Binding IsPersonalConveyance, Mode=TwoWay}"
                          Margin="0,0,10,0"/>

            <ToggleButton Style="{StaticResource DutyTileWideStyle}"
                          Content="Yard&#x0a;Move"
                          IsChecked="{Binding IsYardMove, Mode=TwoWay}"/>
        </UniformGrid>

    </StackPanel>
</UserControl>