namespace OverWatchELD.Services
{
    /// <summary>
    /// Minimal placeholder to satisfy build.
    /// If you already have a real log persistence class elsewhere, we can later redirect this.
    /// </summary>
    public sealed class LogStore
    {
        public LogStore() { }
    }
}