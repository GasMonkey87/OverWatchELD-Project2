namespace OverWatchELD.Services
{
    /// <summary>
    /// Minimal placeholder to satisfy build.
    /// If you already have a real HOS rules engine elsewhere, we can later wire this to it.
    /// </summary>
    public sealed class HosRulesService
    {
        public HosRulesService() { }
    }
}